{
  "kind": "collectionType",
  "collectionName": "districts",
  "info": {
    "name": "District"
  },
  "options": {
    "increments": true,
    "timestamps": true
  },
  "attributes": {
    "name": {
      "type": "string",
      "private": false,
      "required": true
    },
    "facilities": {
      "collection": "facility",
      "via": "district"
    }
  }
}
